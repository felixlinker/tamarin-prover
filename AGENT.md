Run `stack build` to compile the project.

You can run regression tests with the following commands.
However, note that regression tests are quite expensive and should thus be run sparingly.
```
rm -rf case-studies
python3 regressionTests.py
```
